<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <title>Listagem de Produtos</title>
</head>
<body>
    <div class="d-flex justify-content-between">
    <h3>Listagem de Produtos</h3>
    <a href="/produtos/create" type="button" class="btn btn-primary">+</a>
    </div>  
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Nome</th>
                <th scope="col">Preço</th>
                <th scope="col">Estoque</th>
                <th scope="col" style="text-align:right">Opções</th>
            </tr>
        </thead>
        <tbody>
          {{#each produtos}}
            <tr>
                <th >{{nome}}</th>
                <td>{{preco}}</td>
                <td>{{estoque}}</td>
                <td style="text-align:right">
                  <a href="/produtos/{{id}}"><i class="bi bi-eye-fill"></i></a>
               <a href="/produtos/update/{{id}}"><i class="bi bi-pencil-fill"></i></a>
               <a href="/produtos/delete/{{id}}"onclick="return confirm('Tem certeza que deseja remover este produto?');"> <i class="bi bi-trash-fill"></i></a>
                </td>
                
            </tr>
            {{/each}}
           
        </tbody>
    </table>
</body>
</html>
